---
/** DEMO ONLY - delete before launch. */
interface Props {
  /** What is fake about this page. */
  what: string;
}
const { what } = Astro.props;
---
<p class="demo-note"><strong>Demo content.</strong> {what}</p>
